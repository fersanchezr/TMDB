/* const api_url = require("./tmdb");
const axios = require('axios')

const movies = (url) => {
    axios(url)
    .then((response) => response.data)
    .then((data) => {
      console.log(data);
    });
};

movies(api_url);

module.exports = movies; */